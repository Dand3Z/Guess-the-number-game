<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#game-welcome-title"
          th:text="#{game.welcome.title}"/>
    <attr sel="#play-game"
          th:href="@{__${T(pl.dele.util.GameMappings).PLAY}__}"
          th:text="#{game.play}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footerFragment}"/>
</thlogic>